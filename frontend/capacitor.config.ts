import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.snackIA.mobile',
  appName: 'snackIA',
  webDir: 'dist',
  server: {
  cleartext: true
}
};

export default config;
